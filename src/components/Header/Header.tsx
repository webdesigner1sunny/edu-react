// import React from "react";

import TopBar from "./TopBar";

const Header = () => {
  return (
    <div>
      <nav className="site-nav mb-5">
        <TopBar />
      </nav>
    </div>
  );
};

export default Header;
